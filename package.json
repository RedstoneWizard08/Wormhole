{
    "name": "@wormhole/workspace",
    "version": "0.0.0",
    "scripts": {
        "fmt": "prettier --write . && cargo fmt",
        "bindgen": "cargo run --bin bindings",
        "dev": "tauri dev",
        "dev:remote": "cargo run --bin webui",
        "build": "tauri build",
        "tauri": "tauri",
        "app:build": "pnpm -C apps/ui run app:build"
    },
    "devDependencies": {
        "@tauri-apps/cli": "^1.5.11",
        "prettier": "^3.3.3"
    }
}
